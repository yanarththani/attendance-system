import React from "react";

const HomePage = () => {
    return (
        <div>
            <h1>Welcome to the Home Page </h1>
            <button >Logout</button>
        </div>
    );
};

export default HomePage;
